# splunk/web.conf
[settings]
# Tell Splunk it's being served from /monitoring path
root_endpoint = /monitoring

# Enable proxy support
tools.proxy.on = True

# Optional: Set server name if needed
server.socket_host = 0.0.0.0